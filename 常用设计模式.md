### 一、单例模式

该模式就是确保一个类在整个程序里有且只有一个实例,在全局也只有一个,在实际开发中,以下场景就适合单例模式:

- 串口管理器
- 日志记录器
- 配置管理器
- 数据库连接池

```c#
public class CommManager
{
    //使用 Lazy<T> 实现线程安全的延迟初始化单例
    private static readonly Lazy<CommManager> _instance =
        new Lazy<CommManager>(() => new CommManager());

    //对外暴露的静态实例属性
    public static CommManager Instance => _instance.Value;

    //封装的 SerialPort 对象
    public SerialPort SerialPort { get; } = new SerialPort();

    //私有构造函数，防止外部通过 new 创建实例
    private CommManager() { }

    //打开串口的公共方法，带有默认参数
    public void Open(string port = "COM3", int baud = 9600)
    {
        SerialPort.PortName = port;
        SerialPort.BaudRate = baud;
        SerialPort.Open();
    }
}

//调用示例,位于其他类的方法中
CommManager.Instance.Open("COM6", 115200);
```


### 二、观察者模式

该模式就是一个对象的状态变了,所有关心它的对象都能自动收到通知,常见的使用场景如下:

- 传感器的数据更新界面显示
- 设备状态变化通知多个模块
- 报警事件广播


```c#
public class CommManager
{
    //定义事件：使用 EventHandler<byte[]> 委托，表示事件触发时会传递 byte[] 类型的数据
    public event EventHandler<byte[]> DataReceived;

    //内部处理方法：通常绑定到 SerialPort.DataReceived 系统事件上
    private void SerialPort_DataReceived(object sender, SerialDataReceivedEventArgs e)
    {
        //从串口读取所有可用字节
        byte[] data = new byte[((SerialPort)sender).BytesToRead];
        ((SerialPort)sender).Read(data, 0, data.Length);

        //触发自定义事件：通知所有订阅者有新数据了
        //?.Invoke 是线程安全的写法，防止没有订阅者时报错
        DataReceived?.Invoke(this, data);
    }
}

//---以下是外部调用/订阅示例---
//订阅事件：当收到数据时，执行 UpdateChart 方法
CommManager.Instance.DataReceived += (s, data) => UpdateChart(data);

//订阅事件：同时执行日志记录
CommManager.Instance.DataReceived += (s, data) => Log(data);

//订阅事件：同时执行协议解析
CommManager.Instance.DataReceived += (s, data) => ParseProtocol(data);
```

这也是发布-订阅模式，发布者负责“生产”数据。当底层串口硬件发来数据时，它通过 SerialPort_DataReceived 捕获，然后像广播一样通过 DataReceived?.Invoke(...) 把数据扔出去。它不关心谁在听，只管发。
外部的订阅者通过 += 符号注册自己的处理函数。这里展示了多播委托的特性——同一个事件可以挂载多个处理函数（画图、写日志、解析协议互不干扰，同时执行）。





### 三、策略模式






### 四、工厂模式



### 五、MVVM模式
