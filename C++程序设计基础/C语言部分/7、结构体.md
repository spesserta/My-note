# 一、结构体的声明

>如下图就是一个标准的声明形式：其中person表示结构体的数据类型，timmy是声明结构体时顺便创建的一个结构体变量。结构体内部花括号里面的变量表示结构体的成员。下面的struct person是C语言中在其他区域创建结构体时固定加上的数据类型，struct表示这是一个结构体，person表示数据类型是person，剩下的表示变量名。

```c
#include<stdio.h>
struct person {     //表示人信息的结构体
	char name[20];  //名字
	int gender;     //性别
	double weigh;   //体重
	double heigh;   //身高
}timmy; //定义结构体的时候顺便定义一个timmy的人
int main() {
	struct person Jack;   //根据person结构体定义一个叫Jack的人
	struct person Mary; 
	retrun 0;
}
```


>可以通过打点的方式访问或者修改或者赋值结构体变量的成员。

```c
#include<stdio.h>
struct person {     //表示人信息的结构体
	char name[20];  //名字
	int gender;     //性别
	double weigh;   //体重
	double heigh;   //身高
}timmy; //定义结构体的时候顺便定义一个timmy的人
int main() {
	//打点给timmy信息赋值
	timmy.name = "timmy";
	timmy.gender = 1;
	timmy.weigh = 110.12;
	timmy.heigh = 170.22;
	//输出timmy的信息
	printf(timmy.name);
	printf(" %d %f %f",timmy.gender,timmy.heigh,timmy.weigh);
	retrun 0;
}
```

# 二、初始化结构体
> 结构体的初始化和数组的初始化类似：<br>

> 结构的初始化列表的写法需要注意如下4点：
>- 初始化列表由花括号包括。
>- 花括号内为结构成员需要被初始化的值。
>- 初始化值按照结构成员声明时的顺序依次排列
>- 每个初始化值之间由逗号分隔。

```c
#include<stdio.h>
struct person {     //表示人信息的结构体
	char name[20];  //名字
	int gender;     //性别
	double weigh;   //体重
	double heigh;   //身高
}; 
int main() {
	struct person Jack = {"Jack",1,123.22,180.12};  //初始化结构体
	
	retrun 0;
}
```

# 三、结构体数组

>和普通数组一样的操作

```c
#include<stdio.h>
struct person {     //表示人信息的结构体
	char name[20];  //名字
	int gender;     //性别
	double weigh;   //体重
	double heigh;   //身高
}; 
int main() {
	struct person people[3] = {   //申明结构体数组并且初始化
		{"Timmy",1,170,60 },
	    {"Jack" ,0,121,129},
		{"Mary" ,1,122,123},
	};
	 
	for (int i = 0;i < 3;i++) {     //输出结构体数组需要申明一个中间变量
		struct person per = people[i];
		printf("%s",   per.name);
		printf("%d",   per.gender);
		printf("%.2f", per.weigh);
		printf("%.2f", per.heigh);
	}

	retrun 0;
}
```

# 四、嵌套结构体

>一个结构体作为单独变量在另外一个结构体的声明内就是嵌套结构体。初始化的时候内部对应位置多加一个花括号即可。>访问的时候也相应的多加一个点号即可。

```c
#include<stdio.h>
struct privacyi {  //表示人隐私信息的结构体
	char phone[20];
	char email[20];
	unsigned int Id;
};
struct person {     //表示人基础信息的结构体
	char name[20];  //名字
	int gender;     //性别
	double weigh;   //体重
	double heigh;   //身高
	struct privacyi p;
}; 

int main() {
	
	struct person timmy = {
		"timmy", 1, 170.22 , 60.00,
		{"18270729122", "28728121@qq.com", "2182761882121"}
	};

	//输出隐私信息
	printf(timmy.p.email);
	printf("\n");
	printf(timmy.p.phone);
	printf("\n");
	retrun 0;
}
```

# 五、指针指向结构体

>和指向普通变量一样，需要用到取地址符&   <br>

```c
#include<stdio.h>
struct privacyi {  //表示人隐私信息的结构体
	char phone[20];
	char email[20];
	unsigned int Id;
};
struct person {     //表示人基础信息的结构体
	char name[20];  //名字
	int gender;     //性别
	double weigh;   //体重
	double heigh;   //身高
	struct privacyi p;
}; 

int main() {
	
	struct person timmy = {
		"timmy", 1, 170.22 , 60.00,
		{"18270729122", "28728121@qq.com", "2182761882121"}
	};

	struct person* pTimmy = &timmy;  //指向结构体变量的指针
	retrun 0;
}
```

>访问内部成员时C语言有一个更直观的符号->  <br>

```c
#include<stdio.h>
struct privacyi {  //表示人隐私信息的结构体
	char phone[20];
	char email[20];
	unsigned int Id;
};
struct person {     //表示人基础信息的结构体
	char name[20];  //名字
	int gender;     //性别
	double weigh;   //体重
	double heigh;   //身高
	struct privacyi p;
}; 

int main() {
	
	struct person timmy = {
		"timmy", 1, 170.22 , 60.00,
		{"18270729122", "28728121@qq.com", "2182761882121"}
	};

	struct person* pTimmy = &timmy;  //指向结构体变量的指针
	printf("%s",pTimmy->name);   //通过指针来访问结构体变量

	retrun 0;
}
```

# 六、结构体在函数中传递


>结构体的变量类型可以作为形参实参和返回值进行传递，在变量类型前面加上struct即可让C语言知道这是一个结构体。


```c
#include<stdio.h>
struct privacyi {  //表示人隐私信息的结构体
	char phone[20];
	char email[20];
	unsigned int Id;
};
struct person {     //表示人基础信息的结构体
	char name[20];  //名字
	int gender;     //性别
	double weigh;   //体重
	double heigh;   //身高
	struct privacyi p;
}; 

struct person defaultInit() {  //返回值为结构体类型
	struct person per;
	strcpy(per.name,"default"); //给结构体变量赋初始值
	per.gender = 1;
	per.heigh = 170.21;
	per.weigh = 212;
	return per;
}

int main() {
	
	struct person Jimmy;
	Jimmy = defaultInit();  //返回值是结构体类型，接收返回值的变量也应该是结构体类型

	retrun 0;
}
```

# 七、共用体union
## 1、结构体和共用体的内存排列

>根据实践可知结构体的内存排布情况如下：<br>
>结构体所占空间的大小等于或者大于各数据成员的内存大小之和。

<img width="924" height="163" alt="image" src="https://github.com/user-attachments/assets/5b3c207a-0e32-4d0a-a159-34354e8ed1cb" />


>可以看出char和short之间有1个空格，short和long long 之间有4个空格字节，这种情况就是内存对齐，虽然会浪费一些空间，但是有助于结构体更快的访问数据成员。<br>
>以下是共用体的排列规则：

<img width="887" height="432" alt="image" src="https://github.com/user-attachments/assets/ebe333ac-c1a0-4b86-b3dd-c806f3fd32dc" />

<img width="912" height="482" alt="image" src="https://github.com/user-attachments/assets/1facc92c-c938-4324-a6ad-d039cbc075ad" />


>可以看出3个数据类型共享一个内存空间，共用体的大小取决于数据类型最大的变量所占用的内存空间，这就意味着存储一个成员后，将覆盖掉其他成员的数据。<br>
>共用体也有它的用处，假设要用结构体存储一个数据，但是这个数据可能是char型可能是int或者其他类型，这时候就可以用上共用体来存储这个不确定类型的数据。<br>

>用结构体表示共用体如下：

```c
struct message {  //不确定是什么类型的信息message
	int type;     //type表示类型，例如1是int型2是float型3是string型
	int n;
	float f;
	char* str;
};

void printMsg(struct message msg) {
	switc(msg.type) {
		case 1:
			printf("%d\n",msg.n);
			break;
		case 2:
			printf("%f\n", msg.f);
			break;
		case 3:
			printf(msg.str);
			break;
	}
}

int main() {
	
	struct message msg[3];
	//第一个信息是int
	msg[0].type = 1;
	msg[0].n = 123;
	//第二个信息是float
	msg[1].type = 2;
	msg[1].f = 212.21F;
	//第三个信息是string
	msg[2].type = 3;
	msg[2].str = "siqyg";

	for (int i = 0;i < 3;i++) {
		printMsg(msg[i]);
	}
	retrun 0;
}
```

>很显然，每个信息中有2个变量是闲置的，如果用共用体union就能将三个类型合而为一

```c
struct message {
	int type;
	union {
		int n;
		float f;
		char* str;
	}u;
};
```

>注意type 成员是一定需要有的，否则无法判断是什么类型的信息。所以，它不能合并进入 union 。拿到消息后，同样也需要根据消息的 type 使用不同的方式处理。确定 type 后，再从 msg 中找到 union 成员 u ，再根据类型，选择对应的成员进行处理。

```c
struct message {
	int type;
	union {
		int n;
		float f;
		char* str;
	}u;
};

void printMsg(struct message msg) {
	switc(msg.type) {
		case 1:
			printf("%d\n",msg.n);
			break;
		case 2:
			printf("%f\n", msg.f);
			break;
		case 3:
			printf(msg.str);
			break;
	}
}

int main() {
	
	struct message msg[3];
	//第一个信息是int
	msg[0].type = 1;
	msg[0].n = 123;
	//第二个信息是float
	msg[1].type = 2;
	msg[1].f = 212.21F;
	//第三个信息是string
	msg[2].type = 3;
	msg[2].str = "siqyg";

	for (int i = 0;i < 3;i++) {
		printMsg(msg[i]);
	}
	retrun 0;
}
```

# 八、枚举体enum

>假设我们用数字来代表一种数据类型，1代表整型，2代表浮点，3代表字符串。如果数字多了之后呢就很难分辨它对应哪个数据类型。这时候就可以用枚举体来解决。

<img width="527" height="779" alt="image" src="https://github.com/user-attachments/assets/d89f32d8-1148-4b3e-bbad-182fdd264ba9" />


>如上图0代表整形1代表浮点等等，如果想从0开始也可以设置：


<img width="485" height="210" alt="image" src="https://github.com/user-attachments/assets/253c5abd-ee3b-4a50-ac09-c98a0704a6b4" />



>同时也可以专门制定某一个数据类型的数值：


<img width="334" height="141" alt="image" src="https://github.com/user-attachments/assets/0947e37e-2b65-4247-951f-9943463df00c" />


<img width="462" height="532" alt="image" src="https://github.com/user-attachments/assets/7567a742-6b83-4d55-9914-da91fc00c0e4" />

















































































































































































































